import styled from "@emotion/styled";

export const SquareWrapper = styled.div`
  align-items: center;
  background-color: transparent;
  box-shadow: inset 0 0 0 0.5px var(--bgColor);
  display: flex;
  height: 100%;
  justify-content: center;
  position: relative;
  width: 100%;
  z-index: 0;
`;
